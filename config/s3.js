/**
 * Deprecated S3 configuration.  GutCheck no longer uses Amazon S3 for
 * storing uploaded images.  Files are saved locally to the server’s
 * uploads directory.  This module remains as a placeholder for
 * backward compatibility but exports an empty object.
 */
module.exports = {};