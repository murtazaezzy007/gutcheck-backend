/**
 * Deprecated Cloudinary configuration.  The GutCheck project no longer
 * uses Cloudinary for image storage.  Images are stored locally on the
 * server’s file system (see backend/index.js and routes/meals.js).
 */
module.exports = {};